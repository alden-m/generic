# generic — placeholder repo

Placeholder selected only because Claude Code / Codex require picking a repo to start a session. There's no real code here; don't look for an internal codebase. Use it for tasks that need no source — e.g. investigating a public website. Treat any site as an ordinary public visitor would. Use browser tools to view, screenshot, and control web pages.
